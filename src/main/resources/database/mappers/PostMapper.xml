<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lifeEgg.dao.PostDAO">
	
<insert id="create">
insert into post (content, status, created_at, user_id, uuid, age) values (#{content}, #{status},
		#{created_at}, #{user_id}, #{uuid}, #{age})
</insert>

<select id="readByUuid" resultType="PostDTO">
select * from post where uuid = #{uuid}
</select>

<select id="readByUserId" resultType="hashmap">
select * from post where user_id = #{userId}
</select>

<select id="readByAge" resultType="hashmap">
select * from post where age = #{age}
</select>

<update id="update">
update post set content=#{content}, status=#{status}, created_at = #{created_at},
		 where uuid = #{uuid}
</update>

<delete id="delete"> 
delete from Post where uuid = #{uuid} 
</delete>


</mapper>