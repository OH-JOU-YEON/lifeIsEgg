<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lifeEgg.dao.PostDAO">
	
<insert id="create">
<<<<<<< Upstream, based on origin/master
insert into post (content, status, created_at, user_id, uuid) values (#{content}, #{status},
		#{created_at}, #{user_id}, #{uuid}})
</insert>

<select id="read" resultType="PostDTO">
select * from user where id = #{id}
</select>

<update id="update">
update post set content=#{content}, status=#{status}, created_at = #{created_at},
		 where id = #{id}
</update>

<delete id="delete"> 
delete from Post where id = #{id} 
=======
insert into post (user_name) values (#{userName})
</insert>

<select id="read" resultType="UserDTO">
select * from user where id = #{userId}
</select>

<update id="update">
update post set user_name=#{userName}, age=#{age} where id = #{userId}
</update>

<delete id="delete"> 
delete from Post where uuid = #{uuid} 
>>>>>>> dac3a51 로그인 컨트롤러 작성
</delete>


</mapper>